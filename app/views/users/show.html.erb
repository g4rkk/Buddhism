<h1 class="text-3xl font-bold mb-6"><%= current_user.username %>さんのマイページ</h1>

<div class="space-y-6">
  <div>
    <h2 class="text-2xl font-semibold mb-4">合計瞑想時間: <%= @total_meditation_time %> 分</h2>
  </div>

  <div>
    <h3 class="text-xl font-semibold mb-2">最近の冥想セッション</h3>
    <% if @recent_sessions.any? %>
      <ul class="list-disc pl-5 space-y-2">
        <% @recent_sessions.each do |session| %>
          <li>
            ガイド: <%= session.meditation_guide.title %> - 瞑想時間: <%= session.duration_seconds / 60 %> 分
            (<%= session.created_at.strftime('%Y/%m/%d %H:%M') %>)
          </li>
        <% end %>
      </ul>
    <% else %>
      <p>まだ冥想の記録がありません。</p>
    <% end %>
  </div>
</div>
