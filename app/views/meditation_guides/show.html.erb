<!-- app/views/meditation_guides/show.html.erb -->
<div class="container mx-auto py-10">
  <h1 class="text-4xl font-bold text-center"><%= @meditation_guide.title %></h1>
  <p class="text-lg mt-4 text-center text-gray-600"><%= @meditation_guide.description %></p>

  <div class="mt-6 text-center">
    <%= link_to 'このガイドを選択', new_meditation_session_path(meditation_guide_id: @meditation_guide.id), class: 'btn btn-accent' %>
  </div>

  <div class="mt-6 text-center">
    <%= link_to '瞑想ガイド一覧に戻る', meditation_guides_path, class: 'text-primary hover:underline' %>
  </div>
</div>